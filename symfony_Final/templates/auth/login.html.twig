<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="mb-4 text-center">Login</h2>

                    {% if error %}
                        <div class="alert alert-danger" role="alert">
                            {{ error }}
                        </div>
                    {% endif %}

                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_label(form.identifier, 'Username or Email', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.identifier, {'attr': {'class': 'form-control', 'placeholder': 'Enter your username or email'}}) }}
                            {{ form_errors(form.identifier) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.password, 'Password', {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.password, {'attr': {'class': 'form-control', 'placeholder': 'Enter your password'}}) }}
                            {{ form_errors(form.password) }}
                        </div>

                        <div class="d-grid">
                            {{ form_widget(form.login, {'attr': {'class': 'btn btn-primary'}}) }}
                        </div>
                    {{ form_end(form) }}

                    <p class="mt-3 text-center">
                        Not registered yet? <a href="{{ path('home_index') }}">Create an account</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

</body>
</html>